{
  "name": "@enheritage/auth",
  "version": "0.1.0",
  "private": true,
  "description": "Auth0 JWT validation service for the Enheritage platform",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@fastify/jwt": "^8.0.0",
    "@fastify/rate-limit": "^9.0.0",
    "@fastify/sensible": "^5.0.0",
    "fastify": "^4.28.0",
    "fastify-plugin": "^4.5.0",
    "jwks-rsa": "^3.1.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@enheritage/config": "workspace:*",
    "@enheritage/types": "workspace:*",
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.5.0",
    "vitest": "^1.6.0"
  }
}
